[Nemo Action]
# An action to create a new directory on right click of an existing directory.
# Will request password authentication if elevated privileges are needed to complete the action.
# Requires companion script create_dir_admin.sh
Name=Create _Directory (Admin)
Comment=Create a directory in %F elevating privileges in necessary
Exec=<action_scripts/create_dir_admin.sh %F>
Selection=s
Extensions=dir;
Icon-Name=folder-new-symbolic
Dependencies=mkdir;
EscapeSpaces=true
